# Contributing

1. Create a branch from `main`.
2. Copy `config.example.js` to `config.js` and set your Spotify Client ID.
3. Run `npm run check` before opening a pull request.
4. Describe the changed behavior clearly and add a screenshot for UI changes.

Do not publish Spotify tokens, profile data or a personal `config.js`.
